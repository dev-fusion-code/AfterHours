services:
  - type: web
    name: huddle
    env: python
    buildCommand: ./build.sh
    startCommand: daphne -b 0.0.0.0 -p $PORT huddle.asgi:application
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
